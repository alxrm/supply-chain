import times from 'lodash.groupBy';

export const ProductUtils = {

  /**
   *
   * @param productMap
   * @returns {{
   *  [groupdId]: [{ product1 }, { product2 }]
   * }}
   */
  splitProductsByGroups(productMap = {}) {
    return {};
  }
};

export default ProductUtils;