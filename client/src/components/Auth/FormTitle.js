import 'react';
import styled from 'styled-components';

const FormTitle = styled.p`
  text-align: center;
  font-size: 64px;
  font-weight: 600;
  margin: 0 0 56px;
  color: white;
`;

export default FormTitle;