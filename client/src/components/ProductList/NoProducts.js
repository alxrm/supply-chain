import 'react';
import styled from 'styled-components';

export const NoProductsBlock = styled.div`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%,-50%);
  text-align: center;
`;

export const NoProductsLabel = styled.div`
  font-size: 24px;
  padding: 16px;
  color: rgba(11, 31, 53, .87);
`;
